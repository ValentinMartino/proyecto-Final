#include<stdio.h>
#include"funciones.h"

int main()
{
	//ptrNodo ptrInicial = NULL;
	//ptrNodo ptrFinal = NULL;
	
	/*int cant_nodos;
	int opcion; //para manejar las opciones en el do while
	
	cant_nodos = leer_puerto();*/
	
	
	//for(int i = 0; i<11 ; i++)
	//	ingresar_cola(&ptrInicial, &ptrFinal,i);
		
	
	/*
	do{
		printf("\n1)Ver los datos obtenidos por los sensores."
		       "\n2)Guardar los datos obtenidos en un archivo."
		       "\n3)Recuperar lo datos de la ultima vez que se ejecuto el programa."
		       "\n4)Ver el grafico de temperatura."
		       "\n5)Ver el grafico de presion."
		       "\n6)Ver el grafico de humedad."
		       "\n7)Salir.");
	       
		scanf("%i", &opcion);
		
		getchar(); //para el '\n' que queda en el buffer cuando se elije una opcion
		
		if(opcion > 7 || opcion < 1)
			printf("\nEL CARACTER INGRESADO NO CORRESPONDE A UNA OPCION VALIDA. INTENTELO DE NUEVO.\n");
		
		switch(opcion)
		{
			case 1:   break;
			case 2:   break;
			case 3:   break;
			case 4:   break;
			case 5:   break;
			case 6:   break;
			case 7: printf("\nCerrando programa...\n"); break;
			
			default: 
	}while(opcion != 7);*/
	
	
	
	//para ver como funca
	//mostrar(ptrInicial);
	//archivo_usuario(ptrInicial);
	
	leer_puerto();
	
	
	putchar('\n');
	return 0;
}


